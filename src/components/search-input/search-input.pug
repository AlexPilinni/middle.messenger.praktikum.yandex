mixin search-input(className)
    form(class=`search-input ${className}`)
        input.input(
            id=search
            placeholder='Поиск'
        )
        div.icon

        label.label(
            for=search
        ) Поиск


